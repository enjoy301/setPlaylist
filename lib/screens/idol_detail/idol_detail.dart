import 'package:flutter/material.dart';

class IdolDetailScreen extends StatefulWidget {
  const IdolDetailScreen({Key? key}) : super(key: key);

  State<IdolDetailScreen> createState() => _IdolDetailScreen();
}

class _IdolDetailScreen extends State<IdolDetailScreen> {
  @override
  Widget build(context) {
    return Scaffold(
      body: SafeArea(
        child: Text(
          "wow",
        ),
      ),
    );
  }
}
